{
    "required_headers": {
        "strict": false,
        "headers": ["data.quantity", "user.name", "user.email"]
    },
    "formatted_arguments": {
        "data.quantity": "int",
        "user.name": "str",
        "user.email": "str"
    },
    "greater_than_eq": {
        "data.quantity": 1
    },
    "lesser_than_eq": {
        "data.quantity": 1
    }
}